import { style } from "@vanilla-extract/css";

export const section = style({
  paddingInline: "0",
});

export const timeslotParagraph = style({
  display: "flex",
  justifyContent: "space-around",
  flexWrap: "nowrap",
});
